import math

def main() -> None:
    x: float = 16.0
    print(math.sqrt(x))
    print(math.floor(3.7))
    print(math.ceil(3.2))
    print(math.pi)
    print(math.sin(0.0))
    print(math.log(math.e))
    a: float = 3.0
    b: float = 4.0
    print(math.hypot(a, b))
    print(math.pow(2.0, 10.0))
