class Counter:
    count: int
    name: str
    def __init__(self) -> None:
        self.count = 42
        self.name = "my_counter"

def main() -> None:
    c: Counter = Counter()
    print(c.count)
    print(c.name)
