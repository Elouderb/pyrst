def f(x: int) -> int:
    match x:
        case y if y > 10:
            y = y * 2
            return y
        case y:
            y = y + 1
            return y


def main() -> None:
    print(f(20))
    print(f(5))
