# NEGATIVE (W5-a F5): `bytes += str` is invalid Python (TypeError). The augmented-
# assignment path now routes a bytes operand through the explicit bytes-operator
# typing, so this is an HONEST check-time error (the polished message ordinary
# `bytes + str` gives), not a late codegen error or an rustc leak.
def main() -> None:
    b: bytes = b"ab"
    b += "cd"
    print(b)
