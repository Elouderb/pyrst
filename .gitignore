# Rust build output
/target
*.swp
.DS_Store

# pyrst build outputs: compiled ELF binaries and transpiler output emitted at
# the repo root by `pyrst build`. These are regenerable artifacts and must
# never be committed.
/main
/dict_methods
/final_intertype_test
/verify_init
/test_*
# ...but keep the test harness and recorded results (these are not binaries)
!/test_all.sh
!/test_results.txt

# graphify generated knowledge-graph output (regenerated on demand)
/graphify-out/
/p
*.long-type-*.txt
